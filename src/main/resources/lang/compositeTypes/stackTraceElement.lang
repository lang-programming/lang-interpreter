lang.version = v1.0.0
lang.name = Lang Standard Implementation (&StackTraceElement)

struct &StackTraceElement {
    $path{?TEXT}
    $file{?TEXT}
    $pos{STRUCT} # TODO replace type constraint with "&CodePosition"
    $className{?TEXT}
    $functionName{?TEXT}
    $modulePath{?TEXT}
    $moduleFile{?TEXT}
}
